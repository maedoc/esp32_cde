name: ESP32 CDE CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        
    - name: Build with ESP-IDF
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.1.2
        target: esp32
        path: '.'
        command: |
          idf.py build
          
    - name: Run basic compilation tests
      run: |
        ./test_compile.sh
        
    - name: Test project structure
      run: |
        ./scripts/test_suite.sh

  qemu-test:
    runs-on: ubuntu-latest
    needs: build-and-test
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        
    - name: Install QEMU
      run: |
        sudo apt-get update
        sudo apt-get install -y qemu-system-xtensa
        
    - name: Build and test with QEMU
      uses: espressif/esp-idf-ci-action@v1
      with:
        esp_idf_version: v5.1.2
        target: esp32
        path: '.'
        command: |
          idf.py build
          timeout 30 ./scripts/test_qemu.sh || echo "QEMU test completed"

  python-training:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r python/requirements.txt
        
    - name: Run Python training tests
      run: |
        cd python
        python test_cde_training.py
        
    - name: Run MDN and MAF tests
      run: |
        cd python
        python cde_training.py --test --no-plot